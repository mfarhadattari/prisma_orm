const rawQuery = async () => {
  /* const data =
    await prisma.$queryRaw`SELECT * FROM "users" WHERE username LIKE 'shan%'`;
  console.log(data); */
  // await prisma.$queryRaw`TRUNCATE TABLE "categories" CASCADE`;
};

export default rawQuery;
