import { PrismaClient } from "@prisma/client";

export const prisma = new PrismaClient();

const main = async () => {
  // await find();
  // await create();
  // await update();
  // await deleteOperation();
  // await upsert();
  // await pagination();
  // await sorting();
};

main();
